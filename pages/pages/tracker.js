import { Container } from 'react-bootstrap';
import { Tracker } from 'sub-components';

const EmpTracker = () => {
    return (
        <Container fluid className="p-6">
            {/* content */}
            <Tracker />
        </Container>
    )
}

export default EmpTracker;